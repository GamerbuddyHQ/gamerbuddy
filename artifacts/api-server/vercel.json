{
  "version": 2,
  "installCommand": "cd ../.. && pnpm install && pnpm --filter @workspace/api-server run build",
  "buildCommand": "",
  "functions": {
    "api/index.js": {
      "maxDuration": 30,
      "memory": 512
    }
  },
  "rewrites": [
    { "source": "/:path*", "destination": "/api" }
  ]
}
